FROM node:20-alpine

WORKDIR /app

# Copy server package dependencies
COPY server/package*.json ./
RUN npm ci --only=production

# Copy server code
COPY server/ ./

ENV PORT=5000
EXPOSE 5000

CMD ["node", "server.js"]
