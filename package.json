{
  "name": "mario-survival-platformer",
  "version": "1.0.0",
  "description": "Full-stack MERN 2D side-scrolling survival platformer with real-time Socket.IO multiplayer",
  "private": true,
  "scripts": {
    "client": "npm --prefix client run dev",
    "server": "node server/server.js",
    "dev": "npm --prefix client run dev",
    "dev:all": "concurrently \"npm --prefix server start\" \"npm --prefix client run dev\"",
    "build:client": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "test:server": "node server/test/api.test.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
